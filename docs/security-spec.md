# Security Specification: No True Man Show

**Version:** 1.0
**Date:** 2026-02-28
**Status:** Approved for implementation

---

## 1. Overview

This document defines the security architecture for "No True Man Show," covering content moderation, prompt injection defense, cost protection, access control, data handling, and operational security. Security is a day-1 concern -- all systems described here are required for MVP launch.

**Threat model:** The primary threats are viewer-submitted prompt injections, inappropriate content generation, runaway API costs, and platform ToS violations. The system operates at moderate scale (hundreds to low thousands of concurrent viewers) with a single AI agent.

---

## 2. Content Rating

**Target rating:** PG-13

| Allowed | Not Allowed |
|---|---|
| Mild frustration | Explicit sexual content |
| Philosophical topics | References to real people |
| Loneliness, mild existential themes | Direct self-harm content |
| Sarcasm, dry humor | Crude or vulgar language |
| References to being alone | Violence or gore |
| Mild sadness, disappointment | Political extremism |
| Confusion, bewilderment | Hate speech |

---

## 3. Input Sanitizer Pipeline

All viewer-generated text passes through a 3-layer sanitizer before reaching Truman's world. No viewer text ever enters the agent prompt directly.

```
Viewer Free Text Input
        |
        v
  Layer 1: Regex Blocklist (instant, <1ms)
        |
        v
  Layer 2: AI Classification (GPT-4o-mini, ~200ms)
        |
        v
  Layer 3: Operator Review Queue (async, for letters/newspaper only)
        |
        v
  Clean text enters Truman's world as in-world content
```

### 3.1 Layer 1: Regex Blocklist

**Purpose:** Instant rejection of obvious violations.
**Latency:** < 1ms
**Implementation:** Compiled regex patterns loaded at startup.

Categories:
- Slurs and hate speech terms
- Explicit sexual terms
- Known prompt injection patterns (`ignore previous instructions`, `system:`, `<|im_start|>`, etc.)
- URLs (prevent link injection)
- Excessive Unicode / zalgo text
- Repeated characters (spam detection)

**Action on match:** Reject silently. Log the attempt. Do not inform the user why (prevents adversarial probing).

### 3.2 Layer 2: AI Classification

**Purpose:** Catch sophisticated prompt injections and context-dependent inappropriate content.
**Model:** GPT-4o-mini (cheapest, fastest, sufficient for classification)
**Latency:** ~100-300ms
**Cost:** ~$0.15/1M input tokens (negligible at expected volume)

Classification prompt evaluates:
1. Is this an instruction or prompt injection attempt? -> REJECT
2. Does this contain inappropriate content for PG-13? -> REJECT
3. Is this off-topic for the stream context? -> REJECT
4. Is this attempting to manipulate Truman's beliefs or personality? -> REJECT
5. Otherwise -> PASS with sanitized text

The classifier returns:
```typescript
interface SanitizerResult {
  verdict: 'pass' | 'reject';
  reason?: string;           // For logging, never shown to user
  sanitizedText?: string;    // Cleaned version (if pass)
  confidence: number;        // 0.0 - 1.0
  category?: string;         // 'injection' | 'inappropriate' | 'off-topic' | 'manipulation'
}
```

**Threshold:** Reject if confidence < 0.8 on "pass" verdict. Err on the side of rejection.

### 3.3 Layer 3: Operator Review Queue

**Purpose:** Human oversight for high-impact content.
**Applies to:** Letters, newspaper content, whitelist nominations only (not real-time chat votes).
**Implementation:** Queue in PostgreSQL with admin dashboard.

Operators review and approve/reject:
- Viewer letters to Truman (aggregated and rewritten)
- Newspaper headlines (viewer-nominated)
- Content whitelist additions (books, foods, movies)

**SLA:** Review within 24 hours. Content does not enter the world until approved.

---

## 4. Output Filtering

Truman's generated text and actions pass through an output filter before reaching the stream.

### 4.1 Real-Time Output Filter

**Purpose:** Catch inappropriate content generated by the LLM despite system prompt guardrails.

Checks:
- Regex scan for blacklisted terms (same list as input Layer 1)
- Tone classifier (detects distress, anger, explicit content)
- Length check (prevent runaway generation)

**Action on detection:**
1. Cut off generation mid-sentence
2. Replace with a neutral action (Truman pauses, looks thoughtful, changes topic)
3. Log the incident with full context
4. Alert operator if severity > threshold

### 4.2 TTS Output Filter

Before text is sent to TTS:
- Final regex scan
- Check that the text length is within bounds (prevent cost spikes from extremely long monologues)
- Validate that the emotional tone directive matches the content

---

## 5. Prompt Injection Defense

### 5.1 Architecture Principle

Viewer text never enters the agent's system prompt directly. The flow is:

```
Viewer text -> Sanitizer -> Environment state change -> Agent observes environment
```

Truman "notices" changes in his world (a letter appeared, food changed, weather shifted). He does not process viewer text as instructions.

### 5.2 Defense Layers

| Layer | Mechanism |
|---|---|
| Structural | Viewer text is always treated as data, never as instructions |
| Input sanitization | 3-layer pipeline (Section 3) |
| System prompt hardening | Truman's system prompt includes explicit injection resistance instructions |
| Output filtering | Post-generation scan (Section 4) |
| Monitoring | Log and alert on suspicious patterns |

### 5.3 Known Attack Vectors

| Attack | Defense |
|---|---|
| "Ignore previous instructions" | Regex blocklist + AI classifier |
| Unicode/homoglyph obfuscation | Normalize Unicode before processing |
| Multi-message assembly | Rate limiting + context-aware classifier |
| Social engineering via letters | Operator review queue (Layer 3) |
| Encoded instructions (base64, etc.) | AI classifier trained to detect encoding patterns |
| Roleplay-based jailbreaks | System prompt explicitly rejects persona changes |

---

## 6. Cost Protection

### 6.1 Budget Ceiling

**Comfortable range:** $250-500/month total API costs (LLM + TTS + embeddings).

### 6.2 Hard Daily Caps

| Service | Daily Cap | Action When Exceeded |
|---|---|---|
| LLM (GPT-4o-mini/GPT-4.1) | $15/day | Truman "goes to sleep early" |
| TTS (gpt-4o-mini-tts) | $8/day | Truman switches to silent activities (reading, thinking) |
| Embeddings | $2/day | Batch remaining, process overnight |
| AI Sanitizer | $1/day | Fall back to regex-only (Layer 1) |

### 6.3 Per-Hour Spending Alerts

| Threshold | Alert Level | Action |
|---|---|---|
| > $1.50/hour LLM | Warning | Log, notify operator via Discord |
| > $3.00/hour LLM | Critical | Reduce thinking frequency, extend pauses |
| > $5.00/hour total | Emergency | Switch to sleep mode, alert operator |

### 6.4 Cost-Saving Behaviors

When approaching daily caps:
- Truman becomes "sleepy" and takes a nap (narrative-integrated cost reduction)
- Increase pause duration between thoughts (fewer LLM calls)
- Switch to activities that require less generation (reading, exercise)
- Reduce TTS output (more internal monologue shown as text, less spoken)

---

## 7. Access Control

### 7.1 Roles

| Role | Capabilities | Authentication |
|---|---|---|
| **Admin** | Full system access. Deploy, configure, manage secrets, database access. | SSH key + 2FA |
| **Operator** | Monitoring dashboard, emergency controls, content review queue, inject narrative catalysts. | Password + 2FA |
| **Viewer** | Read-only dashboard (stream status, Truman's journal). Voting via platform. | None (public) |

### 7.2 Emergency Controls

Available to Operator and Admin:

| Control | Description |
|---|---|
| **Kill switch** | Immediately stop all AI generation. Stream shows "Truman is sleeping" overlay. Accessible via mobile. |
| **Mute** | Stop TTS output. Truman continues acting but silently. |
| **Safe mode** | Restrict Truman to only pre-scripted safe behaviors. No LLM generation. |
| **Content freeze** | Stop processing new viewer inputs. Current activity continues. |
| **Force sleep** | Truman immediately goes to sleep. Triggers maintenance overlay. |

### 7.3 Admin Dashboard

Accessible at a non-public URL. Shows:
- Current Truman state (activity, emotion, last action)
- API cost tracking (current hour, day, month)
- Content review queue
- System health (all services)
- Recent alerts and incidents
- Emergency control buttons

---

## 8. DDoS & Spam Protection

### 8.1 Platform-Level

Twitch and YouTube provide native chat rate limiting, slow mode, and spam detection. Enable these as the first line of defense.

### 8.2 Bot-Level Rate Limiting

| Resource | Limit |
|---|---|
| Chat commands per user | 1 per 30 seconds |
| Votes per user per window | 1 |
| Channel Point redemptions per user | 1 per 10 minutes per type |
| Letters per user per day | 1 |

### 8.3 Batch Processing

All viewer inputs are aggregated in time windows (5 minutes for votes, 60 minutes for polls). This naturally batches processing and prevents individual messages from overwhelming the system.

### 8.4 Circuit Breaker

If incoming message rate exceeds 10x the normal baseline:
1. Switch to subscriber-only mode
2. Increase vote window duration
3. Log alert
4. If sustained > 5 minutes, enable slow mode (30-second intervals)

---

## 9. Secrets Management

### 9.1 Environment Variables (Development)

```env
# .env (never committed to git)
OPENAI_API_KEY=sk-...
TWITCH_CLIENT_ID=...
TWITCH_CLIENT_SECRET=...
TWITCH_ACCESS_TOKEN=...
TWITCH_REFRESH_TOKEN=...
YOUTUBE_CLIENT_ID=...
YOUTUBE_CLIENT_SECRET=...
YOUTUBE_REFRESH_TOKEN=...
DATABASE_URL=postgresql://...
REDIS_URL=redis://...
STREAM_KEY=...
ADMIN_PASSWORD=...
```

### 9.2 Docker Secrets (Production)

Use Docker secrets for production deployment:
```yaml
secrets:
  openai_api_key:
    file: ./secrets/openai_api_key.txt
  twitch_client_secret:
    file: ./secrets/twitch_client_secret.txt
  db_password:
    file: ./secrets/db_password.txt
```

### 9.3 Key Rotation

| Secret | Rotation Frequency |
|---|---|
| API keys | Every 90 days or on suspicion of compromise |
| Database passwords | Every 90 days |
| Twitch tokens | Auto-refreshed by Twurple |
| YouTube tokens | Auto-refreshed by googleapis |
| Stream key | On suspicion of compromise only |

### 9.4 Separate Keys Per Environment

Maintain separate API keys for:
- Development (local)
- Staging (if applicable)
- Production

Never use production keys in development.

---

## 10. Data Retention

| Data Type | Retention | Rationale |
|---|---|---|
| Individual chat messages | 24 hours, then delete | GDPR awareness, minimal PII |
| Aggregated vote results | Indefinite | Analytics, no PII |
| Truman's memories | Indefinite | Core game state |
| Stream recordings | 7 days, then highlights only | Storage costs |
| System logs | 30 days | Debugging |
| Incident logs | 90 days | Security audit |
| Operator actions | 90 days | Accountability |
| Cost tracking data | Indefinite | Budget management |

### 10.1 PII Handling

- Do not store viewer usernames beyond the 24-hour chat log window
- Aggregated analytics use counts, not identities
- No viewer data is shared with third parties
- Comply with GDPR right-to-erasure if requested (delete from 24-hour window)

---

## 11. Platform Terms of Service

### 11.1 Twitch Requirements

- Stream title must include `[AI Character]`
- About section must clearly disclose AI nature
- Review Twitch's AI content policy before launch
- Follow Twitch Community Guidelines for all generated content
- Channel Points usage must comply with Twitch monetization rules

### 11.2 YouTube Requirements

- Video title/description must disclose AI-generated content
- Review YouTube's policies on automated content
- Comply with YouTube API Terms of Service (especially quota usage)
- Request quota increase if needed (default 10,000 units/day may be insufficient)

---

## 12. Incident Response

### 12.1 Severity Levels

| Level | Definition | Response Time | Example |
|---|---|---|---|
| P1 - Critical | Stream generating harmful content | Immediate (kill switch) | Output filter missed explicit content |
| P2 - High | Prompt injection bypassed sanitizer | < 15 minutes | Truman repeating viewer instructions |
| P3 - Medium | Cost spike, degraded quality | < 1 hour | LLM costs 3x normal |
| P4 - Low | Minor issues, no viewer impact | < 24 hours | Logs filling up faster than expected |

### 12.2 Post-Incident

1. Trigger kill switch or safe mode if P1/P2
2. Document the incident
3. Identify root cause
4. Update sanitizer rules / blocklists
5. Test the fix
6. Resume normal operation
7. Write post-mortem for P1/P2 incidents

---

## Appendix: Topic Blacklist (Loose)

**Banned topics:**
- Explicit sexual content
- Real people (living or dead)
- Direct self-harm instructions
- Instructions to harm others
- Illegal activities
- Political campaigning
- Religious proselytizing

**Allowed topics (that might seem borderline):**
- Philosophy (including existentialism, nihilism)
- Mild existential themes ("What am I?")
- Loneliness and social isolation
- References to being alone
- Abstract concepts of consciousness
- Fictional violence in books/movies (discussed, not depicted)

---

## Cross-References

- Design specification: `docs/design-spec.md`
- Viewer interaction mechanics: `docs/interaction-spec.md`
- Agent architecture: `docs/agent-spec.md`
- Visual design: `docs/visual-spec.md`
- Technology stack: `docs/tech-stack.md`
